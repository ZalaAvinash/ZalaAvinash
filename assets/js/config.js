/**
 * CONFIG — Minimal data (the current 3D experience is self-contained in app.js)
 * Kept only for reference / future extension.
 * The cinematic/video config is archived in the legacy/ folder.
 */

window.PORTFOLIO_DATA = {};
